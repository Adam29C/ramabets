import React from "react";
import Chart_Container from "./Chart_Container";
const PanaChart = (props) => {
  return (
    <div>
      <Chart_Container {...props} />
    </div>
  );
};

export default PanaChart;
